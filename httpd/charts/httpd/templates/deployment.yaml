apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "httpd.fullname" . }}
  labels:
    {{- include "httpd.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "httpd.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "httpd.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "httpd.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: envoy-sidecar
          image: envoyproxy/envoy:v1.37-latest # Using a modern 2026-era version
          ports:
            - name: http-envoy
              containerPort: {{ .Values.sidecar.port }}
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # env:
          # - name: JWKS_REFRESH_INTERVAL
          #   value: {{ .Values.jwksRefreshInterval }}
          command: ["/bin/bash"]
          args:
            - -c
            - >-
                /scripts/run.sh
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
          {{- range $path, $_ :=  .Files.Glob  "index/**" }}
          {{- $file := $.Files.Get $path }}
            - mountPath: /sources/{{ $path }}
              name: index-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
              subPath: {{ base $path }}
          {{- end }}
          {{- range $path, $_ :=  .Files.Glob  "httpd-cfg/**" }}
          {{- $file := $.Files.Get $path }}
            - mountPath: /sources/{{ $path }}
              name: httpd-cfg-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
              subPath: {{ base $path }}
          {{- end }}
          {{- range $path, $_ :=  .Files.Glob  "scripts/**" }}
          {{- $file := $.Files.Get $path }}
            - mountPath: /scripts
              name: script-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
          {{- end }}
          {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
            - mountPath: /opt/app-root/src
              name: cache-volume
      volumes:
      {{- range $path, $_ :=  .Files.Glob  "index/**" }}
      {{- $file := $.Files.Get $path }}
        - configMap:
            name: index-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
          name: index-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
      {{- end }}
      {{- range $path, $_ :=  .Files.Glob  "httpd-cfg/**" }}
      {{- $file := $.Files.Get $path }}
        - configMap:
            name: httpd-cfg-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
          name: httpd-cfg-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
      {{- end }}
      {{- range $path, $_ :=  .Files.Glob  "scripts/**" }}
      {{- $file := $.Files.Get $path }}
        - configMap:
            name: script-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
            defaultMode: 0777
          name: script-{{ $path | sha256sum | trunc 10 }}-{{ $file | sha256sum | trunc 10 }}
      {{- end }}
        - name: cache-volume
          emptyDir: {}
      {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
